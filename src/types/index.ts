export interface MenuItem {
  title: string;
  to: string;
  icon: string;
}

export interface Project {
  title: string;
  description: string;
  tags: string[];
  icon: string;
}

export interface Skill {
  icon: string;
  title: string;
  description: string;
}